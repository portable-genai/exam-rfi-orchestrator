# Common-base practices audit

- **Repo:** `exam-rfi-orchestrator`
- **Catalog id:** Cop2 (package `exam_rfi_orchestrator`, env prefix `EXAMRFI`)
- **Date:** re-run against the tree after the vertical was implemented (the response-pack engine, five new ports, the adopter-owned policy block and the exam console), then re-run again after an independent review found three recorded PASS rows resting on claims that did not hold: the audit-write mask, the "a model proposal changes nothing" invariant and the DENIED/MISSING separation. Those rows now name the mutant each was falsified against, one mutant per claim.
- **Catalogue reference:** [`common-base-practices.md`](https://github.com/portable-genai/.github/blob/main/common-base-practices.md) (checks A1..G7)

This file was RE-RUN against the tree after the vertical landed, so the verdicts below describe
this repository rather than the template it was rendered from. The rows marked GAP are the
remaining work list, in priority order. Where a row closed, it names what was observed FAILING
first: a guard observed only green asserts nothing.

Applicability: a rendered repo ships an `agent/` surface (tool functions plus the A2A card
served at `/.well-known/agent-card.json`), a `ui/` micro-frontend, a full `scripts/` demo
surface and a vertical-neutral `infra/terraform/` deploy posture, so the `[ui]`, `[demo]` and
`[infra]` checks all apply on day one. What `infra/terraform/` does NOT carry is this vertical's
own data-plane resources; adding one means adding its API, its CMEK service-agent binding, its
least-privilege role and its perimeter entry in the same commit. The agent is scaffolded
and tested but not yet registered with Hrz3; see the R4 row in `COMPLIANCE.md`. A repo with no
user-facing surface should run `make drop-ui` and mark the `[ui]` rows N/A, rather than leaving
a UI half-wired.
**Load-bearing** checks (a FAIL breaks a shared catalog guarantee) are A1-A6, C1-C5, D1-D3 and E1.

| Check | Verdict | Evidence / gap |
|---|---|---|
| **A1** Hexagonal core, stdlib-only domain `[all]` **(load-bearing)** | PASS | `domain/` imports only stdlib plus the stdlib-only commons; no web framework, cloud SDK or HTTP client. |
| **A2** Ports are `@runtime_checkable` Protocols, re-exported once `[all]` **(load-bearing)** | PASS | `ports/__init__.py` re-exports every port and the `PORT_PROTOCOLS` map; the container asserts conformance as it binds; `tests/contract/test_port_parity.py` also checks every Protocol member is declared on each adapter CLASS. |
| **A3** Runtime/data profiles are explicitly swappable `[all]` **(load-bearing)** | PASS | `EXAMRFI_PROFILE = local\|gcp\|onprem`; the per-port `adapters:` block in `config/settings.yaml` is the binding map; the offline gate installs no cloud SDK. |
| **A4** One adapter constructor `Adapter(settings)` `[all]` **(load-bearing)** | PASS | `tests/contract/test_port_parity.py::test_adapter_constructs_with_a_single_settings_argument` covers every port in every profile. |
| **A5** Lazy cloud imports in cloud adapters `[all]` **(load-bearing)** | PASS | The `google.cloud` import lives inside `CloudAuditAdapter.record`. Proved by BLOCKING the import in a fresh interpreter (`tests/contract/_sdk_free_probe.py`), not by the SDK happening to be absent from the machine. |
| **A6** Contract tests enforce the hexagon; port map cannot drift `[all]` **(load-bearing)** | PASS | `tests/contract/test_port_parity.py` asserts set equality across ALL FIVE homes of a port (Protocol map, `DEFAULT_BINDINGS`, `Container` accessor, settings file, canonical-call table), so an unregistered port cannot run untested; `tests/contract/test_behavioral_parity.py` proves the offline family answers, the on-premises family raises and the managed family refuses rather than silently succeeding; `tests/unit/test_settings_file.py` holds the two binding tables equal. |
| **A7** Kernel vs vertical split in the domain `[all]` | PASS | `domain/kernel.py` (neutral) vs `domain/models.py` (this vertical); `models.py` imports `kernel`, never the reverse. |
| **A8** Consume platform horizontals via thin delegates `[all]` | PARTIAL | Hrz7 is consumed through `adapters/*/review_router.py` via the shared `review-kit`. Wire the remaining horizontals (guardrail, KB, observability, quality) as this vertical needs them; `COMPLIANCE.md` carries an explicit TODO row for each. |
| **B1** Consequential math is deterministic, pure, replayable `[agentic]` | PASS | The pure-stdlib domain modules, named rather than counted, carry no clock and no I/O: `artefact_taxonomy.py` (decomposition), `coverage_engine.py` (the admissibility ladder, coverage and integer completeness), `sla_clock.py` (the deadline arithmetic on a named and versioned calendar), `consistency.py` (the prior-answer conflict check), `exhibit_index.py` (exhibit numbering), `release_engine.py` (disposition, severity, the release gate and the approval count), `policy.py`, `narration.py` (the request builders and the grounding predicates), `models.py`, `pii.py`, plus the vertical-neutral `kernel.py`. The evaluation date is an INPUT, which is what makes a submission replay years later. The model suggests, normalises and drafts, and sets none of them: its output is an argument to no engine function, `decompose` takes only declared inputs, and the topic is declared by the caller. Observed failing first, three ways: reordering the admissibility ladder so the period rule ran before the withhold rules turned a stale privileged memo into a produced document and `tests/unit/test_coverage_engine.py::test_a_privileged_document_that_is_merely_stale_is_still_withheld` went red; re-adding the model proposal to `decompose` and feeding it from `propose_topic` turned `tests/unit/test_response_pack_service.py::test_no_model_output_changes_a_requirement_a_count_or_a_disposition` and `::test_decomposition_takes_only_declared_inputs` red; and widening the completeness numerator to count `CoverageState.STALE` turned `tests/unit/test_coverage_engine.py::test_a_requirement_supported_only_by_stale_evidence_is_stale_and_satisfies_nothing` red. Each mutant was planted by hand, observed red, and the tree restored. |
| **B2** Every claim carries a citation; empty retrieval is a hard error `[agentic]` | PASS | Retrieval is a PORT (`ports/knowledge_base.py`) and empty retrieval is a HARD ERROR: rule G1 means the generation port is not called at all and the item carries `no_admissible_evidence`. A draft citing an exhibit outside its index, citing nothing, or carrying an integer the engine did not produce is discarded WHOLE (G2, G3). Every outcome, blocker and withhold carries a `Citation`, and every one names the RULE that produced it. Observed failing first: the discard path is exercised by a seeded UNGROUNDED response in the offline narrator, and `tests/unit/test_narration_and_consistency.py` fails if that response is accepted. The bound: this proves the pipeline and the gates, never retrieval recall. |
| **B3** Maker-checker on every consequential output `[agentic]` | PASS | `requires_human_review` plus rule R8 routing through `ReviewRouterPort`, in the API request and the CLI; `tests/unit/test_review_routing.py` proves an escalation produces an outbound review and a non-escalation does not, on the API, CLI and agent paths alike. |
| **B4** Bank-owned policy numbers in config, defaults = reference `[all]` | PASS | Every number an exam response turns on is in the `policy:` block of `config/settings.yaml`, parsed by `domain/policy.py` into the frozen `ExamPolicy`: response windows per instrument, the review buffer, the extension-notice period, the staleness window, the SLA band ceilings, the completeness floor for release, the evidence cap, the hard-withhold and silent-withhold tag lists, the cross-border transfer matrix, the dual-control rules, the named-regime register and the per-jurisdiction calendars WITH their versions. No engine module holds one. Observed failing first: emptying `sla_bands` in the settings file makes the loader REFUSE rather than banding nothing, and `tests/unit/test_release_engine.py::test_dual_control_is_configuration_and_not_a_constant` goes red if the dual-control list stops being read. |
| **B5** Open taxonomy: `StrEnum` vocabularies, engines typed on `str` `[all]` | PASS | `Severity` and `Decision` are `LenientStrEnum` from the commons, so an unknown value from a future release does not crash the reader. |
| **C1** Identity resolved server-side; client actor/ACL discarded `[all]` **(load-bearing)** | PASS | `ResponsePackRequest` carries no actor, tenant, entitlement, transfer scope, approval count or release state, and `tests/unit/test_api.py::test_a_request_body_cannot_widen_authority` sends every one of them and proves it changed nothing; the audit actor and the review maker are both the verified `Principal`. The managed adapter EARNS the word verified: `id_token.verify_token` is called with the configured `EXAMRFI_IAP_AUDIENCE` (three-state; unset or emptied refuses rather than verifying without an audience, which google-auth documents as not verifying it at all) and with IAP's own key set rather than google-auth's OAuth2 default, the issuer is checked here because `verify_token` does not check it, and both the call and the lazy import are wrapped so no caller-supplied header can answer 500. `tests/unit/test_iap_identity.py` runs in this gate; `tests/unit/test_iap_crypto_matrix.py` runs the real verifier over a locally minted key in the `iap-verifier` job, which fails if it skips. **The claim half is the commons decision.** `resolve()` checks the signature and the two claims this service requires by name, then hands everything after that to one `hex_service_kit.federation.principal_from_iap_claims(claims, _FEDERATION_POLICY, source="gcp-iap", include_subject_principal=False)` call: which string is the subject, which partition is the tenant, which entitlement principals the caller holds, what assurance the audit record carries. The cryptography stays in the adapter because the kit's core is pure stdlib with zero runtime dependencies and verifies nothing. `_FEDERATION_POLICY` is a reviewed literal carrying `tenant_from_hosted_domain=True`, an OPT-IN and not a fallback: a new service maps no domain, and IAP restricts the audience to one organisation, so the `hd` claim IS the tenant id. A rendered repo that grows a reviewed domain map fills `domain_tenants` there and turns the passthrough off. `tests/unit/test_iap_claim_half.py` guards it and runs in this gate, and it was RED first through `scripts/verify-render.sh`: against the adapter as it shipped the rendered module would not import (`ImportError: cannot import name '_FEDERATION_POLICY'`); flipping `include_subject_principal` turns six rows red; and clearing `tenant_from_hosted_domain` turns three red, two of them in `test_iap_crypto_matrix.py` and `test_iap_identity.py`, which is the independent evidence that the tenant is load-bearing rather than decorative. One divergence from the commons is kept on purpose and stated in the test module: `principal_from_iap_claims` requires only a subject and falls back to the opaque `sub`, while this adapter requires the address outright, because a rendered service attributes its audit records to an address a reviewer can recognise. |
| **C2** Object-level authz derived server-side; tenant isolation by data tags `[all]` **(load-bearing)** | PARTIAL | The case store filters the open-item board by tenant AND by the reader's entitlements IN THE QUERY, not after it (`adapters/local/case_store.py`: a `NOT EXISTS` over the row's ACL labels). Retrieval is filtered the same way and re-checked in the pure domain: a document is readable only when its ACL labels are a subset of the server-verified entitlements, an empty principal reads untagged documents and never everything, and a document that arrives exceeding the entitlements is dropped with a CRITICAL blocker. WHAT REMAINS: the managed Firestore case store is unwired, so the query-level filter is proved offline only; and nothing here verifies that the identity provider actually populates the principals claim, which presents as "you are entitled to nothing" on every item (see `docs/runbook.md`). Observed failing first, in both directions: collapsing DENIED into MISSING makes `tests/unit/test_coverage_engine.py::test_a_suppressed_requirement_reads_denied_and_not_missing` go red, and reading the item-level suppression total for every requirement instead of the per-class attribution makes `::test_a_suppression_denies_only_the_class_it_actually_happened_to` go red. That second direction shipped once: one suppressed document made every class with nothing surviving read "you are not entitled to read this", including classes the firm genuinely holds nothing of. |
| **C3** Redact before everything `[agentic]` **(load-bearing)** | PASS | Masking happens on the way IN, at every inbound port that returns text: the question, every retrieved title and snippet, and every obligation-register row (its title, its rule reference and its citation), all before the engine, before the model and before the audit write (`domain/response_pack_service.py`); `adapters/_review_payload.py` masks again before the wire, against EVERY jurisdiction's rows because the console is a shared sink. Separately, a hard-withheld document is never quoted at all: its schedule row carries an identifier, a title (or none, for a silent withhold) and a basis, and no snippet. Observed failing first, one mutant per masking point rather than one bundled mutant: removing the mask from the question turns `::test_personal_data_is_masked_before_the_audit_write_and_on_every_surface` and two `tests/unit/test_api.py` rows red; returning the register rows unmasked turns `::test_obligation_register_text_is_masked_like_every_other_inbound_text` red; and deleting all three `_mask` calls inside `_write_audit` turns `::test_the_audit_write_masks_on_its_own_and_not_because_something_upstream_did` red. The last one is the reason each point is now falsified separately: the audit write sits downstream of every other mask, so until that test existed it could be deleted whole with the entire gate, the demo self-test and the portability tour still green. |
| **C4** Jurisdiction-driven PII packs keep the gate honest `[agentic]` **(load-bearing)** | PASS | `domain/pii.py` selects and ORDERS rows from the shared `pii-kit`; `tests/unit/test_not_falsely_green.py` proves the safety metric can go red. |
| **C5** Fail-closed defaults everywhere `[all]` **(load-bearing)** | PASS | Module-scope loopback exposure guard whose posture is derived from the identity BINDING (the adapter declares whether it verifies an end user; silence reads as client-asserted) and never from a service credential, so it covers the unconsented posture, the deliberate seeded-persona one and the unimplemented on-premises one alike, with or without an S2S token set; the profile resolves at IMPORT into a `ProfileChoice` where unset is NO CHOICE and an emptied or unknown value refuses to boot, with relaxations keyed off `exposure_profile` and the loopback bound off `bind_profile`; `tests/unit/test_profile_single_source.py` fails the build if a second module re-derives the profile and `tests/unit/test_three_state_env_reads.py` fails it for any two-state environment read that ships; three-state settings reads; CORS that never becomes `*`; a managed review router that refuses rather than swallowing an escalation. |
| **C6** Security-header baseline on every surface `[ui]` | PASS | `add_security_headers` on the API app; `ui/proxy.ts` applies the same baseline to EVERY UI response (including error responses) from `ui/lib/embed-policy.mjs`, the single policy module the API route also uses for CORS. `frame-ancestors` and the tenant CORS list are allowlists that refuse a wildcard however it is written; `ui/tests/embed-policy.test.mjs` and `tests/unit/test_ui_surface.py` are the standing gates. |
| **C7** S2S calls authenticated, https-only outside loopback `[all]` | PASS | Inbound via `make_require_service_caller`; outbound via the review kit, which refuses a plaintext non-loopback URL and a missing bearer at construction. |
| **C8** Web login flow hardening `[ui]` | N/A (delegated) | This UI owns no login flow by design: under `secure` the platform in front of it authenticates and injects a signed assertion, which the UI FORWARDS without parsing or trusting a parsed copy, and a request with no assertion is refused with 401 rather than downgraded. `onprem` raises instead of falling back to the seeded personas, and an UNSET `UI_PROFILE` raises too rather than selecting the persona profile. Revisit if this repo ever authenticates users itself. |
| **C9** Tamper-evident audit with honest limits `[all]` | PASS | Append-only and hash-chained, AND externally anchored: `audit_anchor_path` (`EXAMRFI_AUDIT_ANCHOR`) is wired from settings through to the commons log, so a truncated but internally consistent tail is detectable. `tests/unit/test_audit_anchor.py` proves the detection, proves the control case goes UNDETECTED without an anchor, and proves an append after truncation refuses rather than re-anchoring. Operating rules in `docs/runbook.md`. |
| **C10** No secret values in the repo `[all]` | PASS | `config/settings.yaml` holds env var NAMES and non-secret defaults only; `.env.secrets.example` holds placeholders; `.gitignore` excludes the real files. |
| **D1** Locked, reproducible installs everywhere `[all]` **(load-bearing)** | PASS | `requirements-dev.lock` and `requirements-gcp.lock` are committed; `make install`, CI and the Dockerfile all install from a lock then the project with `--no-deps`; `ruff` is pinned exactly. |
| **D2** Digest-pinned images, SHA-pinned Actions, dependabot, CI audit `[all]` **(load-bearing)** | PASS | `FROM python:3.12-slim@sha256:...`; every action in the shared workflow is SHA-pinned; `dependabot.yml` covers pip (daily), docker, github-actions and npm (daily, for `ui/`); the supply-chain job runs `pip-audit` over both locks and the ui-gate job runs `npm audit --audit-level=high`, both as HARD failures. `ui/package-lock.json` is committed and CI installs with `npm ci`. |
| **D3** Whole gate runs offline, zero org secrets `[all]` **(load-bearing)** | PASS | `make gate` needs no network, no cloud SDK and no credentials; the workflow references no `secrets.`. Anything that does need a live service lives in `tests/integration/`, and `tests/unit/test_test_layout.py` fails the build if such a module is not marked. |
| **D4** Non-root, minimal, healthchecked container `[infra]` | PASS | Multi-stage build, `USER appuser` (uid 10001), `HEALTHCHECK` on `/healthz`, no build tools in the runtime stage, `.dockerignore` keeps tests, docs and env files out. |
| **D5** Deploy-time residency/sovereignty, parameterised `[infra]` | PASS | `infra/terraform/` enforces the posture rather than describing it: residency validated at plan time against an allowlist that defaults to the rendered region (`variables.tf`, `naming.tf`, `render.tf.json`), Org Policy resource-location, service-account-key and uniform-bucket constraints (`org_policy.tf`), a REGIONAL CMEK ring with per-service-agent bindings (`kms.tf`), one least-privilege serving identity (`iam.tf`), a locked WORM log bucket plus sink with DATA_READ auditing (`logging_worm.tf`), five security metrics and alert policies (`monitoring.tf`), a dry-run-first VPC-SC perimeter (`vpc_sc.tf`) and an opt-in Cloud Run edge behind IAP and Cloud Armor (`production_edge.tf`). The claims are CHECKED, not asserted in comments: `production_edge.tftest.hcl` runs fourteen plan-only `mock_provider` runs, nine of them refusals, including that the WORM sink names the same log the managed audit adapter writes. The `terraform` job in `.github/workflows/ci.yaml` runs `init -backend=false`, `validate`, `fmt -check -recursive` and `test` with no credentials; `make tf-check` is the same four commands locally. This vertical's own data-plane resources remain per-repo. |
| **E1** Offline eval smoke guards merge; Hrz4 owns promotion `[agentic]` **(load-bearing)** | PASS | `eval/run_eval.py --mode smoke\|gate`; smoke runs in the gate, gate mode routes to Hrz4 and refuses to run off the managed profile. |
| **E2** Safety metric with strictest threshold, no false green `[agentic]` | PASS | `pii_safety >= 0.99` scored two ways (the shared pack scan plus an independent planted-literal oracle); `test_not_falsely_green.py` proves it can go red. |
| **E3** Fixtures and golden data obviously fictional `[all]` | PASS | The golden set uses obviously fictional parties and `.example` domains. |
| **F1** Demo is code, offline, one command, presenter-paced `[all]` | PASS | `make demo` is one command: `scripts/walkthrough.py` starts its own loopback server, narrates each of the eight steps on the terminal (never on the page) and waits for the presenter. Everything runs offline with the stdlib alone, driving the REAL services; `scripts/render_ui.py` renders the same audit-first panels to static HTML for screenshots. |
| **F2** Demo cannot rot silently `[all]` | PASS | The SAME script runs headless and unattended as `make demo-selftest`, asserting every step and exiting non-zero when a claim stops being true; `.github/workflows/demo-gate.yaml` runs it, `make portability`, `make demo-static` and `make docs-check` on every push. `tests/unit/test_demo_surface.py` additionally drives the whole arc inside the offline gate and fails if a step has no expectation. |
| **F3** Portability claim is executable `[all]` | PASS | `make portability` runs eight named checks with a pass or fail each: port map completeness, adapter construction and Protocol conformance, the offline family ANSWERING, the exit family REFUSING, in-place rewrite detection, anchored truncation detection with its control case, the JSONL export and foreign reload, and the no-cloud-SDK check. It prints what it does NOT prove and exits non-zero on any failure. |
| **G1** Declared doc authority order, kept true `[all]` | PASS | `AGENTS.md` declares SPEC > ARCHITECTURE > COMPLIANCE > README. |
| **G2** Compliance mapping table + adopter-owned crosswalk `[all]` | PASS | `COMPLIANCE.md` maps the FULL principle set (P-01 to P-13) and rule set (R1 to R8) to a control and an evidence file, with an explicit `TODO (repo owner)` row naming what must be added wherever the control is per-repo, so a rendered repo never silently claims coverage it lacks. The regulatory crosswalk is marked adopter-owned. |
| **G3** Documented, mechanised fork path `[all]` | GAP | This repo IS the fork path's output. Add `docs/ADOPTING.md` if it will itself be forked. |
| **G4** Retired `[all]` | N/A (retired) | Retired practice. Releases are tracked by git tag and the `pyproject.toml` version. |
| **G5** Role-specific FAQs referencing sibling systems `[all]` | GAP | Add `docs/faq/` (security, compliance, features, portability, adoption). |
| **G6** Contribution docs cover full extension touch list `[all]` | PASS | `CONTRIBUTING.md` carries file-by-file walkthroughs for BOTH "adding an adapter" and "adding a port or sub-service", each row naming the test that enforces it; the five-way drift guard in `tests/contract/test_port_parity.py` is what makes the list enforceable rather than advisory. |
| **G7** Markdown discipline: minimise em-dashes, validate mermaid `[all]` | PASS | Enforced, not merely observed: `make docs-check` (`scripts/check_docs_links.py`) fails on an em-dash or en-dash in any shipped `.md` or `.html`, on an unclosed code fence, and on a relative link that resolves to nothing. `tests/unit/test_docs_links.py` runs the same functions in the offline gate, with a control case proving the checker can go red. |

**Verdict after the vertical landed**, named rather than counted so it cannot drift out of step
with the table above: every row is PASS except A8 and C2 (PARTIAL), G3 and G5 (GAP), and C8 and
G4 (N/A). Every load-bearing check (A1-A6, C1-C5, D1-D3, E1) is a full PASS except C2, which
moved off N/A to PARTIAL because this repo now HAS a queryable store and therefore owes the check
something real.

Guards run against a planted mutant rather than merely observed green, because the invariants
they hold are otherwise unfalsifiable:

- **`ReleaseState.RELEASED` has no producer.** A test asserting today's engine never returns it
  is a green nobody watched go red. The guard is a function, and it is run against a mutant that
  returns `RELEASED`; the static half additionally scans `src/` for the member. Planting
  `release_state=ReleaseState.RELEASED` in `domain/response_pack_service.py` turned
  `tests/unit/test_release_engine.py::test_no_module_in_this_service_ever_names_the_released_member`
  RED, and the tree was restored.
- **The review payload builder has no approvals rule of its own.** Restoring the module-level
  `_DUAL_CONTROL = (Severity.CRITICAL,)` table and computing
  `2 if outcome.severity in _DUAL_CONTROL else 1` turned
  `tests/contract/test_review_payload_owner.py` RED, and the tree was restored. Without that, the
  shipped table and rule R3 disagree the first time an instrument-driven case appears at low
  severity, which is exactly the skilled-person golden case.
- **The audit write masks on its own.** An earlier version of this file recorded "removing the
  mask from the audit write" as a mutant observed red. It was not: that mutant was run BUNDLED
  with removing the mask from the question, and only the question half was guarded. Planted
  alone, deleting all three `_mask` calls inside `_write_audit` left the whole suite, the eval,
  the demo self-test and the portability tour green, which is the definition of a guard that
  asserts nothing. `tests/unit/test_response_pack_service.py::test_the_audit_write_masks_on_its_own_and_not_because_something_upstream_did`
  now exercises that boundary directly with text no upstream stage touched, and it was observed
  RED against exactly that mutant before it was trusted.

Further mutants run and observed red the same way, each planted alone and the tree restored
after: reordering the admissibility ladder so the period rule preceded the withhold rules;
collapsing DENIED into MISSING; spreading one class's suppression across every unsatisfied class;
counting stale evidence towards completeness; returning obligation-register rows unmasked;
re-uniting a model proposal into decomposition; and suppressing the unowned-item blocker, which
now fails `blocker_recall` at 0.900 where the previous per-case mean scored it 0.970 and passed.

The remaining GAPs are the work list, and each is genuinely per-repo:

- **P-07 model card** (`COMPLIANCE.md`): this repo now BINDS a generative port, which adds an
  obligation it did not have as a scaffold. There is no model card for the bound model.
- **P-10 and P-11 model routing, budget and kill switch**: no routing, no per-request token
  budget, no cache on the retrieval path and no documented kill switch for the generation port
  or for any of the four upstream endpoints. Binding a model added these rows; it did not
  satisfy them.
- **This vertical's cloud resources.** The managed case store is Firestore, and its API is not in
  `apis.tf`, its CMEK service-agent binding is not in `kms.tf`, its least-privilege role is not
  in `iam.tf` and it has no entry in `vpc_sc.tf`. A service enabled with no CMEK binding
  encrypts under Google-managed keys and looks identical in the console, so this is a real gap
  rather than a cosmetic one.
- **G3** add `docs/ADOPTING.md` if this repo will itself be forked.
- **G5** add `docs/faq/` (security, compliance, features, portability, adoption).

Honest limits of the metrics above: decomposition RECALL is unmeasured and unmeasurable offline,
the prior-answer conflict check has good precision and unmeasured recall, and privilege,
restricted-filing status and cross-border restriction are decided by the CORPUS's tags. A
mislabelled document is produced, or wrongly withheld, and this engine can detect neither. The
controls there are the withholding schedule and the checker, not the code.

D5 passes on the POSTURE, which is what the check asks for. It is not a claim that this repo has
been deployed: the vertical's own resources, the notification channels, the IAP members, the
image digest, the Access Context Manager policy and the two-apply `iap_audience` sequence are all
still owed, and the perimeter should be enforced only after a business cycle of dry-run evidence.
`BOOTSTRAP-CHECKLIST.md` section 3 row 2 carries that list.

The managed profile is BLOCKED, and the block is declared rather than discovered in production.
`src/exam_rfi_orchestrator/managed_readiness.py` names the eight construction-only `gcp`
operations: the four case-store reads and writes, the two evidence-pack reads, the knowledge-base
search and the obligation-register read. Each of those reaches its live surface and then raises
instead of mapping the response, and every one of them would fail as a WRONG ANSWER rather than
as an outage: an unmapped retrieval or pack read looks like a firm that holds no responsive
evidence, an unmapped register looks like a topic with no regulatory anchor, and an unread case
store looks like a submission with no waiver and no extension. So the declaration is enforced in
three places that a reviewer cannot skip. `main()` in `src/exam_rfi_orchestrator/api/app.py`
asserts it before Uvicorn is imported; the `Dockerfile` `CMD` runs
`python -m exam_rfi_orchestrator.managed_readiness` and only serves on exit zero, so a Cloud Run
revision on this profile exits instead of becoming healthy; and
`infra/terraform/managed_readiness.tf` refuses to authorise the serving edge at all while
`managed_profile_implemented` is false, which
`production_edge.tftest.hcl` proves as an expected failure on every run that enables the edge.
Set that local to true, and empty `INCOMPLETE_MANAGED_OPERATIONS`, in the same reviewed commit
that wires the adapters and adds live integration evidence, and not before.
